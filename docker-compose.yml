version: "3.9"
services:
  rtmp:
    build: ./RTMP
    ports:
    - "1935:1935"
    container_name: rtmp_server

  auth:
    build: ./Auth
    container_name: auth_server

  client:
    build: ./ClientServer
    container_name: client_server
